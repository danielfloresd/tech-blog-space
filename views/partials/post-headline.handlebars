<div class="card-header {{bgcolor}} border-success py-1">
    <a href="/post/{{id}}" class=" btn btn-block {{bgcolor}} font-weight-bold
        text-left {{textcolor}} px-0 mx-0">
        <b>👾{{user.username}}</b> - 
        <span style="float:center">{{title}}
        </span>
    </a>
</div>
<div class="card-title">
    <p class="float-left text-info" style="font-size:small">
        <i class="fas fa-tag"></i>
        {{category}}
        <span class="float-right">
            {{ formatDate created_at }}
        </span>
    </p>
</div>
